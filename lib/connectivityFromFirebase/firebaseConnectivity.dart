import "package:flutter/material.dart";
import 'package:hello/connectivityFromFirebase/register.dart';

class FirebaseConnectivity extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Register();
  }
}
